export const environment = {
  production: true,
  apiUrl: '/api',
  apiVersion: 'v1',
  logLevel: 'error',
  features: {
    topicBrowser: true,
    replay: true,
    scriptManager: true,
    encodingValidator: true,
  },
};
